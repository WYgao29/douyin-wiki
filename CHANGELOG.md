# 更新日志

本项目遵循语义化版本格式，并使用 `v<版本号>` 作为 Git 标签。

## 未发布

### 新增

- 废纸篓支持勾选、全选/全不选，可一次恢复或彻底删除多条资料。

- Web 成为日常操作主入口：授权中心区分抖音账号授权与视频下载授权，可从网页发起、检查并在成功后恢复暂停任务。
- 新增导入中心与全局任务中心。单条导入可保存灵感、媒体保留、长视频和 AI 确认；博主与收藏支持清点、跨页选择、确认、历史和失败重试。收藏支持增量/完整更新。
- 任务详情提供统一阶段、下一步操作、时间线、子任务统计、长视频批准和 ASR/OCR 复核。Gateway 模式明确表示为等待外部 Agent，不会说成后台正在自动完成。
- 系统设置提供 Worker/队列健康、doctor、存储统计，以及 maintenance / database rebuild 的预览和确认执行。删除导入记录与清除收藏缓存不会误删已入库资料。

- Web 界面完成 Apple 风格视觉重构：亮暗双主题改用 `#f5f5f7`/纯黑画布与 iOS 主色，侧栏、顶栏、对话面板毛玻璃化，全面加大圆角并使用柔和投影；设计规范见 [Web UI 设计系统](docs/web-ui-design-system.md)。
- 侧栏、顶栏与对话区图标全部重绘为 SF Symbols 风格实心内联 SVG，导航项采用彩色瓷贴并带级联入场、hover 一体染色与选中弹簧动效。
- 新增 TikTok 风品牌 logo（黑底音符 + 青/品红错位重影），应用于主页面与设置页品牌区。
- 对话区重构：紫色渐变 AI 徽标与折叠入口悬浮钮、iOS 风格圆形发送钮、消息与引用卡投影；弹窗磨砂背景与弹性入场。
- Web 资料库新增收藏视图，并可在资料卡片和文章页收藏或取消收藏。
- 收藏的视频永久保留在本地；媒体已被清理时会创建仅下载媒体的恢复任务，不重复执行转录或 AI 分析。

### 修复

- 卡片进入文章的视图过渡不再出现封面拖影与首页闪动：旧封面快速淡出、根图层短促平滑切换，卡片浮动按钮随卡片一体退场。
- 顶栏在窄窗口下搜索框快捷键提示不再溢出挤压按钮。

### 可靠性

- 重复收藏复用同一恢复任务；取消收藏后恢复任务遵循最新状态，认证失败也不会丢失收藏标记。
- SQLite 自动迁移收藏字段，Vault 数据库重建会保留收藏状态。

## 0.1.5 - 2026-08-31

### 修复

- 抖音短链现在跟随完整重定向链，以最终 `/video/` 或 `/note/` 落点识别作品类型，并拒绝作品 ID 冲突和过长跳转链。

### 新增

- Web 资料库支持提交单独的抖音链接或整段分享文案到后台采集队列，并在前后端拒绝非抖音链接。
- 任务因真实授权失败暂停后，可通过 macOS 系统弹框打开对应浏览器；服务器验证授权成功后自动重试同一授权通道中的待授权任务。
- 自动授权引导支持按通道互斥、取消、超时和安全降级，且不会读取或显示 Cookie 值、密码或 Local Storage。

## 0.1.4 - 2026-08-29

### 修复

- 中文检索改用统一的 CJK 单字/双字词法索引，并保留原文子串补充通道。
- 图文采集严格校验响应作品 ID，避免推荐流内容写入错误作品。
- Worker 任务更新增加租约所有者 fencing，artifact 合并改为单事务原子更新。
- 修复博主清单空选择、短视频时长换算、手工导入竞态和丢失媒体 checkpoint。
- SQLite 重建逐文件报告损坏侧车，并可恢复已创建提醒的系统状态。
- 配置保存保留用户注释与未知键，Keychain 密钥不再出现在进程参数中。
- 改进聊天错误分类、长视频确认续跑、OCR 图片归因和 Vault 符号链接处理。
- Keychain 写入补齐确认输入和回读校验，避免成功返回却保存空密钥。
- 中文检索增加低权重宽松召回并统一 FTS、精确子串的评分语义。
- 配置补丁可识别多行 TOML 值，避免把值中的方括号误判为新配置段。
- 嵌入模型运行中失败时停止持久化，杜绝不同向量空间混写。
- 登录判断结合状态码、最终 URL 和结构标记，普通正文不再因登录字样误报。
- Web 聊天数据库操作移出异步事件循环，MCP 错误保留稳定代码与详情。

### 安全与可靠性

- Web 引用链接同时在服务端和浏览器端限制为 HTTP/HTTPS。
- Git 提交失败不再阻断 Vault 与 SQLite 主数据同步。
- CLI 入口提供稳定 JSON 错误结构，并在初始化服务前校验互斥参数。

## 0.1.3 - 2026-08-28

### 新增

- Web 资料页新增“移入废纸篓”，同步保存资料、原始记录、媒体和关联数据，避免直接永久删除。
- 新增废纸篓页面，支持恢复资料以及二次确认后彻底删除。

### 可靠性

- 删除与恢复操作使用单条目锁和阶段化清单，进程中断后可自动回滚或完成，避免资料与数据库状态不一致。
- 恢复资料时同步恢复专题来源、博主作品、提醒、关系、用途和条目聊天上下文。

### 文档

- 新增 MCP 规则下沉设计备忘，记录后续版本的开箱即用 Agent 工作流方案；当前版本暂不改变 MCP 接口。

## 0.1.2 - 2026-08-27

### 变更

- 视频资料页改为按主题开始时间生成“时间轴图解”，支持章节摘要、要点、结构化对比表和可追溯证据。
- 移除“关键片段”输出；旧分析中的关键片段可在读取时兼容转换，重新分析后生成完整章节。
- 兼容旧资料中重复时间戳的关键片段，并拒绝没有有效引文的章节证据。

## 0.1.1 - 2026-08-26

抖库首个本地可交付版本。

### 新增

- 抖音视频与静态图文的统一采集、转录、OCR、复核、分析和 Obsidian 入库流程。
- 博主主页与单作品反查入口、完整作品清点、选择式批量导入和手动同步。
- 分析文档 v2：一屏精华资料页、自动内容分类、知识原子和隐藏机器侧车。
- CLI、MCP STDIO、Gateway Agent、后台 Provider 和本地降级三种分析路径。
- FTS5 与本地中文向量混合检索，返回原作品链接、视频时间戳或图文图片编号。
- 本机 Web v0.1.1：真实封面资料库、文章阅读、流式 AI 对话、引用、聊天历史和模型设置。
- 严格选源的专题研究，以及总览、对比表、证据地图、共识分歧、决策简报和 FAQ 成果。
- macOS 提醒事项确认写入、每周知识维护、媒体保留策略和 Vault 本地 Git 提交。

### 安全与兼容

- 模型密钥仅保存在环境变量或 macOS Keychain，不写入 Vault、SQLite、配置文件或 Git。
- Web 仅监听 `127.0.0.1`，限制 Host、同源请求和 Vault 媒体路径，并清理 Markdown HTML。
- 保留 `douyin-wiki` 命令、Python 包、MCP 标识和状态目录作为稳定技术名称。
- 用户可见状态统一使用中文，用户可见时间统一使用北京时间。

### 已知限制

- 仅支持 macOS 本地单用户和抖音来源。
- 图文暂不处理 Live Photo 动态内容。
- 抖音登录、验证码和页面结构可能随平台变化，需要通过授权状态检查重新验证。
- 真实浏览器与抖音网络测试不在默认离线测试中执行。
- 不包含公网部署、多用户、移动端分享菜单、远程同步或自动 Git 推送。
